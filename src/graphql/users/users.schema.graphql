type User {
  id: ID!
  username: String
  email: String
  pass: String
}

type Query {
  getUsers: [User]
  me: User
}

type Mutation {
  signup (
    username: String!
    email: String
    pass: String,
  ): String
  login (
    email: String
    password: String
  ): String
}