type User {
  id: ID!
  username: String
  email: String
  password: String
}

type Token {
  token: String!
}

type Query {
  getUsers: [User]
  me: User
}

type Mutation {
  signup (
    username: String!
    email: String!
    password: String!
  ): Token!
  signin (
    email: String!
    password: String!
  ): Token!
}